<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Type Analysis</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700;800&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Nunito', sans-serif;
            /* Light Theme: Soft Lavender to Mint Gradient */
            background: linear-gradient(135deg, #e0c3fc 0%, #8ec5fc 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            overflow: hidden; 
            color: #475569; 
            position: relative;
        }

        .blob {
            position: absolute;
            border-radius: 50%;
            filter: blur(40px);
            z-index: 0;
            opacity: 0.6;
            animation: float 10s infinite ease-in-out;
        }
        
        .blob-1 { top: 10%; left: 10%; width: 300px; height: 300px; background: #ff9a9e; animation-delay: 0s; }
        .blob-2 { bottom: 10%; right: 10%; width: 350px; height: 350px; background: #a18cd1; animation-delay: 2s; }
        .blob-3 { top: 40%; left: 40%; width: 200px; height: 200px; background: #fad0c4; animation-delay: 4s; }

        @keyframes float {
            0% { transform: translate(0, 0); }
            50% { transform: translate(20px, -20px); }
            100% { transform: translate(0, 0); }
        }

        .container {
            position: relative;
            z-index: 10;
            background: rgba(255, 255, 255, 0.35); 
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.6);
            border-radius: 30px;
            padding: 3rem;
            width: 100%;
            max-width: 900px;
            box-shadow: 
                0 20px 40px rgba(0, 0, 0, 0.05),
                inset 0 0 0 1px rgba(255, 255, 255, 0.5); 
            transform-style: preserve-3d;
        }

        h1 {
            font-weight: 800;
            font-size: 2.5rem;
            color: #2d3748;
            margin-bottom: 1rem;
            text-align: center;
            letter-spacing: -1px;
            text-shadow: 2px 2px 0px rgba(255,255,255,0.4);
        }

        #displayedText {
            font-family: 'Space Mono', monospace;
            font-size: 1.05rem;
            line-height: 1.8;
            color: #1e293b;
            background: rgba(255, 255, 255, 0.5);
            padding: 1.5rem;
            border-radius: 16px;
            border: 2px dashed #cbd5e1; 
            margin-bottom: 2rem;
            font-weight: 400;
        }
        form {
            display: flex;
            flex-direction: column;
        }

        label {
            font-weight: 700;
            margin-bottom: 0.8rem;
            color: #475569;
            font-size: 1rem;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        label::before {
            content: '';
            display: block;
            width: 8px;
            height: 8px;
            background: #6366f1;
            border-radius: 50%;
        }

        textarea {
            width: 100%;
            background-color: rgba(255, 255, 255, 0.6);
            border: none;
            border-radius: 20px;
            padding: 1.5rem;
            font-family: 'Space Mono', monospace;
            font-size: 1.1rem;
            color: #334155;
            outline: none;
            resize: none;
            box-shadow: 
                inset 6px 6px 12px rgba(166, 180, 200, 0.3),
                inset -6px -6px 12px rgba(255, 255, 255, 0.8);
            transition: all 0.3s ease;
        }

        textarea:focus {
            background-color: rgba(255, 255, 255, 0.9);
            box-shadow: 
                inset 4px 4px 8px rgba(166, 180, 200, 0.2),
                inset -4px -4px 8px rgba(255, 255, 255, 0.9),
                0 0 0 3px rgba(99, 102, 241, 0.3); 
        }

        button {
            margin-top: 2rem;
            padding: 1rem 3rem;
            font-family: 'Nunito', sans-serif;
            font-weight: 800;
            font-size: 1.1rem;
            color: white;
            background: #6366f1; 
            border: none;
            border-radius: 50px;
            cursor: pointer;
            align-self: center;
            box-shadow: 
                0 10px 20px -5px rgba(99, 102, 241, 0.4),
                0 4px 6px -2px rgba(99, 102, 241, 0.2);
            transition: transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        button:hover {
            transform: scale(1.05) translateY(-2px);
            background: #4f46e5;
        }

        button:active {
            transform: scale(0.95) translateY(2px);
        }

    </style>
</head>
<body>

    <div class="blob blob-1" data-speed="2"></div>
    <div class="blob blob-2" data-speed="-2"></div>
    <div class="blob blob-3" data-speed="1"></div>

    <div class="container">
        <h1>Analyse Your Typing Skill</h1>
        
        <h3 id="displayedText">Technology has become an essential part of modern life, influencing the way people communicate, learn, and work. Developing strong typing skills improves efficiency, reduces errors, and enhances overall productivity in digital environments.</h3>
        
        <form>
            <label for="userInput">Start Typing Below</label>
            <textarea name="userInput" id="userInput" rows="6" cols="200" onpaste="return false;" placeholder="Focus here to start..."></textarea>
            <button type="submit">Start Analysis</button>
        </form>
    </div>

    <script>
        document.addEventListener("mousemove", (e) => {
            const blobs = document.querySelectorAll(".blob");
            const x = (window.innerWidth - e.pageX * 2) / 100;
            const y = (window.innerHeight - e.pageY * 2) / 100;

            blobs.forEach(blob => {
                const speed = blob.getAttribute("data-speed");
                blob.style.transform = `translateX(${x * speed}px) translateY(${y * speed}px)`;
            });
        });
    </script>

    <script>
        document.getElementById('userInput').addEventListener('paste', function(e) {
            e.preventDefault();
            alert('Pasting is disabled in this text area.');
        });
        
        document.getElementById('userInput').addEventListener("emptied", function() {
            alert('The text area has been cleared.'); 
        });
    </script>
    <script src="/typing.js" ></script>
</body>
</html>